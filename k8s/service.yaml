apiVersion: v1
kind: Service
metadata:
  name: sre-alert-tracker
  labels:
    app: sre-alert-tracker
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: sre-alert-tracker
